@model MvcReadMe_Group4.Models.User

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<style>

    .scale-wrapper {
        transform: scale(0.85);
        transform-origin: top center;
    }

    .form-wrapper {
        margin-top: 80px;
    }

    .form-container {
        background-color: #1e1b2e; /* deep purple background */
        border-radius: 0px 0px 20px 20px;
        padding: 60px 50px;
        box-shadow: 0 0 20px rgba(75, 0, 130, 0.4); /* indigo shadow */
    }

    .card-header-text {
        background-color: #5a3d8c; /* medium purple header */
        color: #f0f0f0;
        text-align: center;
        padding: 20px;
        font-size: 1.2rem;
        font-weight: 600;
        border-radius: 20px 20px 1px 1px;
    }

        .card-header-text h4 {
            margin: 0;
            font-size: 1.4em;
            font-weight: bold;
        }

    .form-group {
        margin-bottom: 20px;
    }

    .form-control {
        background-color: #2a2340; /* darker purple input bg */
        color: #fff;
        border: 1px solid #5a3d8c; /* purple border */
        border-radius: 8px;
        padding: 6px;
        font-size: 1rem;
        transition: all 0.2s ease;
    }

        .form-control:focus {
            background-color: #352a55; /* brighter focus */
            color: #fff;
            border-color: #8e5eff; /* vibrant purple */
            box-shadow: 0 0 0 0.15rem rgba(142, 94, 255, 0.35);
        }

    .btn-create {
        background-color: #7a42f4; /* vivid purple button */
        color: white;
        font-size: 1.2rem;
        padding: 12px 40px;
        border-radius: 8px;
        border: none;
        cursor: pointer;
        margin-top: 50px;
        transition: background-color 0.3s ease;
    }

        .btn-create:hover {
            background-color: #6933d6; /* deeper purple hover */
        }

    .text-center a {
        color: #9b59b6; /* link purple */
        font-weight: bold;
    }

        .text-center a:hover {
            color: #8e44ad; /* darker purple hover */
        }

</style>


<div class="scale-wrapper">
    <div class="container form-wrapper">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card-header-text">
                    <h4>Create User via Admin</h4>
                </div>
                <div class="form-container">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="form-group mb-3">
                            <label asp-for="UserName" class="control-label text-white">Username</label>
                            <input asp-for="UserName" class="form-control" />
                            <span asp-validation-for="UserName" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Password" class="control-label text-white">Password</label>
                            <input asp-for="Password" class="form-control" type="password" />
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>

                        <div class="form-group text-center">
                            <input type="submit" value="Create" class="btn-create" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div class="text-center mt-4">
        <a asp-action="Index">Back to List</a>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
